# 第1周 Day5-6 线性代数测试题（15 道）

> 覆盖范围：  
> - Day5：向量、矩阵、矩阵乘法、转置、逆矩阵、张量维度  
> - Day6：特征值、特征向量、SVD、PCA、降维思想

---

## 一、选择题（共 5 道）

### 第 1 题

矩阵 $A$ 的形状为 $(3,4)$，矩阵 $B$ 的形状为 $(4,2)$，则 $AB$ 的形状为？

- A. $(3,2)$  
- B. $(4,4)$  
- C. $(3,4)$  
- D. $(2,3)$

---

### 第 2 题

下列关于矩阵转置的性质，哪一个是**错误的**？

- A. $(A^T)^T = A$  
- B. $(AB)^T = A^T B^T$  
- C. $(A + B)^T = A^T + B^T$  
- D. $(kA)^T = kA^T$

---

### 第 3 题

一个方阵 $A$ 可逆的**充要条件**是？

- A. $A$ 的行列式等于 0  
- B. $A$ 的行列式不等于 0  
- C. $A$ 的秩小于 $n$  
- D. $A$ 存在全零行

---

### 第 4 题

对矩阵 $A$ 做 SVD 分解 $A = U \Sigma V^T$，其中 $\Sigma$ 对角线上的值表示什么？

- A. 特征值  
- B. 奇异值  
- C. 方差  
- D. 协方差

---

### 第 5 题

PCA 降维的核心思想是？

- A. 找到数据中方差最小的方向  
- B. 找到数据中方差最大的方向作为主成分  
- C. 对数据做归一化  
- D. 随机选取特征子集

---

## 二、简答题（共 5 道）

### 第 6 题

请解释"向量的内积（点积）"的几何含义，并写出计算公式。

---

### 第 7 题

什么是逆矩阵？在深度学习中，为什么我们很少直接计算逆矩阵？

---

### 第 8 题

在深度学习中，一个 batch 的图像数据通常表示为 4 维张量 `(B, C, H, W)`，请分别解释每个维度的含义，并说明一个 batch_size=32、RGB 图像、分辨率 224×224 的张量形状。

---

### 第 9 题

请用自己的话解释"特征值"和"特征向量"的含义。

---

### 第 10 题

PCA 降维的步骤是什么？请简要列出主要流程。

---

## 三、计算题（共 3 道）

### 第 11 题

给定矩阵：

$$A = \begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix}$$

请计算：
1. $A$ 的转置 $A^T$
2. $A$ 的行列式 $\det(A)$
3. $A$ 是否可逆？如果可逆，写出 $A^{-1}$

---

### 第 12 题

给定矩阵：

$$A = \begin{pmatrix} 2 & 1 \\ 0 & 3 \end{pmatrix}$$

请求出 $A$ 的特征值和特征向量。

---

### 第 13 题

全连接层的输入为 $X \in \mathbb{R}^{32 \times 784}$（32 个样本，784 维特征），权重矩阵为 $W \in \mathbb{R}^{784 \times 256}$，偏置 $b \in \mathbb{R}^{256}$。请问：

1. 输出 $Y = XW + b$ 的形状是什么？
2. 该层共有多少个可学习参数？

---

## 四、代码题（共 2 道）

### 第 14 题

使用 NumPy 完成以下任务：
1. 创建一个 3×3 的随机矩阵 $A$
2. 计算 $A$ 的转置
3. 计算 $A^T A$
4. 判断 $A^T A$ 是否为对称矩阵

---

### 第 15 题

使用 sklearn 对鸢尾花（Iris）数据集进行 PCA 降维，将 4 维特征降到 2 维，并输出前两个主成分的方差解释比例。

---

## 评分标准

| 题型 | 题号 | 每题分值 | 总分 |
|------|------|----------|------|
| 选择题 | 1-5 | 4 分 | 20 分 |
| 简答题 | 6-10 | 8 分 | 40 分 |
| 计算题 | 11-13 | 8 分 | 24 分 |
| 代码题 | 14-15 | 8 分 | 16 分 |
| **合计** | | | **100 分** |
